# Cloudflare Worker URL that forwards form data to Telegram.
# Copy to .env.local for local dev. For production, set as GitHub Actions secret.
REACT_APP_FORM_ENDPOINT=https://your-worker.your-subdomain.workers.dev
